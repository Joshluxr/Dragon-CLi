# Copy to .env.docker and fill with strong values (never commit .env.docker).
# Run: docker compose --env-file .env.docker up -d
#
# Generate secrets, e.g.:
#   openssl rand -base64 32 | tr -d '/+=' | head -c 40

POSTGRES_PASSWORD=
REDIS_PASSWORD=
REDIS_HTTP_TOKEN=
